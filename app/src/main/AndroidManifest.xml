<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.forzo.holdMyCard">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activities.home.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activities.Profile.ProfileActivity" />
        <activity android:name=".ui.activities.sort.SortCardActivity" />
        <activity android:name=".ui.activities.mylibrary.MyLibraryActivity" />
        <activity
            android:name=".ui.activities.login.LoginActivity"
            android:theme="@style/AppThemeNoAction" />
        <activity android:name=".ui.activities.creategroup.CreateGroupActivity"></activity>
    </application>

</manifest>