<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.activities.forgetPasswordConfirmation.ForgetPasswordConfirmationActivity">

    <ImageView
        android:id="@+id/tick_mark"
        android:layout_width="72dp"
        android:layout_height="72dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="48dp"
        android:src="@drawable/ic_confirmation"/>

    <TextView
        android:id="@+id/check_mail"
        android:layout_below="@+id/tick_mark"
        android:layout_width="wrap_content"
        android:layout_marginTop="48dp"
        android:layout_centerHorizontal="true"
        android:textSize="18sp"
        android:textColor="@android:color/black"
        android:layout_height="wrap_content"
        android:text="Check your Email"
        android:textAllCaps="true"/>

    <TextView
        android:id="@+id/reset_pass"
        android:layout_below="@+id/check_mail"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_marginTop="24dp"
        android:textSize="18sp"
        android:text="We Sent you an email with instructions for resetting your password"/>

    <Button
        android:id="@+id/login_password"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="@color/colorPrimary"
        android:text="@string/login"
        android:textAllCaps="false"
        android:textColor="@android:color/white"/>
</RelativeLayout>